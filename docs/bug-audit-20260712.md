---
name: bug-audit-20260712
description: 2026-07-12 全量代码审计发现的 52 个 Bug/问题清单
metadata:
  type: project
---

# 全量代码审计报告 — 2026-07-12

基于 feature/dev 分支全量扫描，共发现 52 个问题。

> 复查日期：2026-07-12 | ✅ = 已修复 | ❌ = 未修复 | 🔧 = 部分修复

---

## 🔴 Critical — 运行必崩（10 个）

| # | 状态 | 文件 | 行 | 问题 | 说明 |
|---|------|------|----|------|------|
| 1 | ✅ | `api_view/servers.py` | 31 | ~~`start_main_agent(user.query, config)` 只传 2 参数~~ | `person` 参数已删除，签名现为 2 参数 `(query, configs)`，调用匹配 |
| 2 | ✅ | `main_agent.py` | 316 | ~~`agent.ainvoke(..., configs=configs)` 参数名错~~ | 已改为 `config=configs`，参数名正确 |
| 3 | ✅ | `orchestrator.py` | 149-153 | `GraphQuery.graph_query()` 缺 `return` | 计算 `graph_result` 但没 return，调用方永远拿到 None |
| 4 | ✅ | `db_operator.py` | 117-130 | `insert_picture` SQL 7 列只传 6 个值 | SQL 有 `source_name` 但 execute 缺对应参数，运行 psycopg2.ProgrammingError |
| 5 | ✅ | `server_main.py` | 6 | `from http_base import mcp_lifespan` | 应为 `from mcp_server.http_base`，非包内导入必崩 |
| 6 | ✅ | `api_view/servers.py` | 9 | `from src.agent.main_agent import start_main_agent` | `src` 不在 sys.path 作为包根，应为 `from agent.main_agent` |
| 7 | ✅ | `server_config.py` | 13,17 | `JAVA_API_BASE_URL` 和 `MCP_PORT` 都是 8000 | Java 后端和 MCP Server 端口冲突，无法同时启动 |
| 8 | ✅ | `graph_traverser.py` | 184-194 | ~~`llm_query` 失败时 `raise ValueError`~~ | 已改为 return 降级，不再 raise 炸流程。注意：返回字符串而非 `[]`，类型不一致，调用方可能拿到 str 而非 list |
| 9 | ✅ | `graph_db/queries.py` | 113-115 | ~~`query_llm` 异常时无 return~~ | 异常返回 None，调用方 `llm_query` 已用 `if not result:` 兜住，不会崩 |
| 10 | ❌ | `graph_traverser.py` | 73-96 | `traverse()` for 循环内 return | 只处理第一个 entity 就退出，多实体查询结果丢失 |

---

## 🟠 High — 功能错误 / 数据丢失（10 个）

| # | 状态 | 文件 | 行 | 问题 | 说明 |
|---|------|------|----|------|------|
| 11 | ✅ | `main_agent.py` | 155 | `create_analyst_middleware(SUMMARY_MODEL, backend)` 传了函数 | `backend` 是函数定义，应传 `backend()` 实例，中间件内部调用会崩 |
| 12 | ✅ | `main_agent.py` | 284-300 | `get_agent_async` 已初始化时返回 `_AgentProxy` | 应返回 `agent._agent`（底层 agent 对象），返回 proxy 导致调用方拿不到正确对象 |
| 13 | ✅ | `knowledge_tools.py` | 38-63 | ~~`graph_rag_search` 参数被吞~~ | 参数已移除，签名只有 `query` + `top_k`，不存在吞参数问题 |
| 14 | ✅ | `knowledge_tools.py` | 131-169 | ~~`graph_query` 参数被吞~~ | 参数已移除，签名只有 `entity`，不存在吞参数问题 |
| 15 | ❌ | `mcp_tools_bean.py` | 280 | `GraphRAGSearchResult.score` 必填 | Pydantic 模型要求 score，但 `knowledge_tools.py` 返回值无此字段，验证必崩 |
| 16 | ✅ | `collections.py` | 152 vs 160 | `PGVectorManager` port 默认值不一致 | `__new__` 默认 5432，`__init__` 默认 54321，实际用哪个取决于调用方式 |
| 17 | ❌ | `graph_traverser.py` + `writer.py` | 模块级 | `N4JD = Neo4jDrivers(...)` import 时连接 | Neo4j 不可达则整个模块导入失败，阻断所有后续代码 |
| 18 | 🔧 | `config.py` | 69-75 | `MongoDBSaver` import 时实例化 | MongoDBSaver 已替换为 PostgresSaver，但仍在模块级实例化，PG 不可达同样导致导入失败 |
| 19 | ✅ | `memory_update.py` | 178 | `_extract_entities` 失败时返回缺键 | 返回 `{"equipment": [], "query": ""}` 缺 `"zones"`，调用方 `.get("zones")` 虽不崩但不一致 |
| 20 | ❌ | `memory_update.py` | 55-105 | `skip_words` 子串匹配误杀 | "ship" 包含 "hi" 会被误判为跳过词，应改用词边界匹配 |

---

## 🟡 Medium — 接口不一致 / 潜在问题（24 个）

| # | 状态 | 文件 | 行 | 问题 |
|---|------|------|----|------|
| 21 | ✅ | `sandbox_setup.py` | 23 | ~~`from src.agent.backends.custom_opensandbox` 应为 `from agent.backends...`~~ | 已修正为 `from agent.backends.custom_opensandbox` |
| 22 | ✅ | `read_yaml.py` | 21 | `from mcp_server import tools` 导入后从未使用，死代码 |
| 23 | ✅ | `config.py` | 22 | `from langchain.chat_models import BaseChatModel` 已废弃，应用 `langchain_core.language_models` |
| 24 | ❌ | `config.py` | 40 | `SCOPE_MAP` 定义后从未使用，死代码 |
| 25 | ❌ | `config.py` | 78 | `SKILLS_STORE_NAMESPACE` 定义后从未使用，死代码 |
| 26 | ✅ | `main_agent.py` | 20-24 | `import uuid` 从未使用 |
| 27 | ✅ | `main_agent.py` | 303 | `class person` 应为 `Person`（PEP 8） |
| 28 | ✅ | `main_agent.py` | 310 | 参数名 `person` 遮蔽类名 `person` |
| 29 | ✅ | `api_view/servers.py` | 14-18 | ~~`thread_id: str` 必填但代码又处理空值生成新 ID，矛盾~~ | 已改为 `str|None=None`，Optional 语义与生成逻辑一致 |
| 30 | ✅ | `api_view/servers.py` | 19 | `class respone` 拼写错误，应为 `response` |
| 31 | ✅ | `MCP_client.py` | 26-31 | MCP Server URL 硬编码，未引用 `server_config.py` |
| 32 | ❌ | `orchestrator.py` | 16-19 | `sys.path.insert` 放在 import 之后，对后续 import 无效 |
| 33 | ✅ | `vector_retriever.py` | 89-103 | ~~`search()` 是 async 但内部调同步方法，会阻塞事件循环~~ | 已用 `asyncio.to_thread` 包装同步调用，不再阻塞事件循环 |
| 34 | ❌ | `db_retriever.py` | 116 | `bm25_search` 参数名 `top_K`（大写K），其他方法用 `top_k`，不一致 |
| 35 | ❌ | `context_fusion.py` | 391 | 中文 Unicode 范围 `'一'~'鿿'` 不完整，缺 CJK 扩展区 |
| 36 | ❌ | `entity_extractor.py` | 289 | docstring 写"2秒超时"，代码实际 `timeout=6.0` |
| 37 | ❌ | `graph_traverser.py` | 98 | 方法名 `by_moudle_query` 拼写错误，应为 `by_module_query` |
| 38 | ❌ | `fire_inspection_tools.py` | 46-49 | `floor` 和 `period` 参数接收但从未使用 |
| 39 | ❌ | `fire_utility_tools.py` | 58-63 | `period` 参数接收但从未使用 |
| 40 | ❌ | `mcp_tools_bean.py` | 183-191 | `FireUtilityMonitorItem` 缺 `type` 字段，但 Mock 数据里有 |
| 41 | ❌ | `config.py` | 47 | `SUMMARY_MODEL = DeepSeek_LLM`，但 `DeepSeek_FAST` 才是摘要/轻量任务专用 |
| 42 | ❌ | `llm_config.py` | 22-25 | 环境变量名不规范（`DEEPSEEKAPI` 应为 `DEEPSEEK_API`），且无 None 校验 |
| 43 | ✅ | `config.py` | 66 | MongoDB 密码硬编码 `root:123456@39.100.100.28` |
| 44 | ❌ | `memory_update.py` | 252-253 | `namespace=(user_id,)` + `key=f"/{user_id}/preferences.md"` 可能导致路径重复 |

---

## 🔵 Low — 代码风格 / 远期优化（8 个）

| # | 状态 | 文件 | 问题 |
|---|------|------|------|
| 45 | ❌ | `memory_update.py:165-169` | JSON 提取用 `find("{")` + `rfind("}")` 极脆弱，应改用 `with_structured_output` |
| 46 | ❌ | `memory_update.py:44-53,365,371,377` | 关键词列表和截断数量硬编码，应外部化 |
| 47 | ❌ | `sandbox_setup.py:110` | 函数名 `create_environment_variables` 实际是创建 venv，名不副实 |
| 48 | ❌ | `sandbox_setup.py:137-160` | 每个文件都 `mkdir -p` + `test -f`，N 个文件 N 次远程调用，性能差 |
| 49 | ❌ | `MCP_client.py:51-58` | `tool.name = f"{key}_{tool.name}"` 原地修改 tool 对象，可能影响缓存 |
| 50 | ❌ | `read_yaml.py:101` | 函数名 `assemble_subagent` 单数，实际处理多个 |
| 51 | ❌ | `test_mcp_tools.py:31` | 导入不存在的 `_MOCK_KNOWLEDGE_DOCS` / `_MOCK_GRAPH_PATHS` |
| 52 | ❌ | `test_mcp_tools.py:639-641` | 测试断言 `result["score"]` 但返回值无此字段 |

---

## 修复统计

| 级别 | 总数 | ✅ 已修复 | ❌ 未修复 | 🔧 部分修复 | 修复率 |
|------|------|-----------|-----------|-------------|--------|
| 🔴 Critical | 10 | 9 | 1 | 0 | 90% |
| 🟠 High | 10 | 6 | 3 | 1 | 60% |
| 🟡 Medium | 24 | 10 | 14 | 0 | 42% |
| 🔵 Low | 8 | 0 | 8 | 0 | 0% |
| **合计** | **52** | **25** | **26** | **1** | **48%** |

---

## 修复优先级建议

1. **先修 Critical 1-10**：不修项目完全跑不起来
2. **再修 High 11-20**：功能逻辑错误
3. **Medium/Low 逐步修**：代码质量和一致性

### Critical 修复关联图

```
#1 servers.py 调用参数 ✅ (person 参数已删除)
#2 main_agent.py ainvoke 参数名 ✅
#3 orchestrator.py 缺 return ✅
#4 db_operator.py SQL 参数 ✅
#5 server_main.py 导入路径 ✅
#6 servers.py 导入路径 ✅
#7 server_config.py 端口冲突 ✅
#8 graph_traverser.py 异常处理 ✅ (已改为 return 降级，但返回 str 非 [])
#9 queries.py 缺 return ✅ (调用方已兜住)
#10 graph_traverser.py 提前 return
```
