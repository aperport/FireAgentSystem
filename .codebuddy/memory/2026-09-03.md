# 2026-09-03

## git pull
- `git pull --ff-only`：Already up to date，远端无新提交（HEAD 仍为 63a6d8c）。

## 待办（承接 2026-09-02，仍未处理）
- `pdf_parser.py::pdf_extract()` 改为写入 `self.md` 不返回值，导致 `pdf_parser_test.py::test_pdf_extract` 断言 `None.strip()` 报错。方案未定：恢复返回值 or 改测试。已向用户提出，等待选择。

## 提交并推送到 GitHub（63a6d8c → 2608304）
- `feat(ingestion)` 1eb1a18：`pdf_parser` 补全 PDF→Markdown 落盘（模块级 `md_output` 目录作默认输出路径，`pdf_write_md()` 按 PDF 文件名生成 .md 并 UTF-8 落盘、返回路径）；`md_parser` 的 `Normalize` 协议与 `NormalizeMD` 归一化方法改为 `@staticmethod`。
- `refactor(graph_rag)` 2608304：`_get_ner_pipeline` 由「全局变量 + asyncio.Lock」改为 `@lru_cache` 懒加载（外部调用方 `knowledge_tools.py` 接口不变）；移除未使用的 `httpx` 导入；`ExtractResult` 示例移入类 docstring；`save_data.py` 移除 `sys.path` 注入 hack，`ingest_markdown`/`ingest_directory` 改为由调用方注入 `MdParser`，LLM 类型收紧为 `ChatOpenAI`。
- 注意：历史中出现 `028c8d4 refactor(graph_rag): implement ReferenceFreeEvaluation...`，非本会话提交（用户在别处提交）。

### 未提交项
- `.vscode/settings.json`（新增 `commentTranslate.multiLineMerge`）属个人编辑器配置，未纳入提交。该文件被跟踪但 `.gitignore` 已含 `.vscode/`，建议执行 `git rm --cached .vscode/settings.json` 使其真正被忽略。
- `.codebuddy/memory/*.md` 保持 untracked。

### 经验补充
- PowerShell 下 `git push` 偶发被判定为长耗时而 skipped，重跑一次即可（实际首次往往已成功，需用 `git status -sb` 复核而非重复推送）。
