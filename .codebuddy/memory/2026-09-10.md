# 2026-09-10

## 同步远程分支
- 在 feature/dev 上执行 `git pull --ff-only`，从 77a887c 快进到 9bf5db7（领先远程 0 落后 2 的提交已合并）。
- 主要变更：graph_rag 多个模块重构（context_fusion / db_retriever / orchestrator / collections / vector_retriever / entity_extractor / knowledge_tools）、pdf_parser 改用 `pdf_inspector`、新增 `src/test/graph_rag_test/` 共 9 个测试文件。
- requirements.txt：移除 rank-bm25（代码中已不再引用，改为自实现/其他 BM25 方案）、`opensandbox-sdk-python` 改名为 `opensandbox`、新增 pdf-inspector、numpy、pytest。
- 已安装缺失依赖 `pdf-inspector` 1.19.0；opensandbox / numpy / pytest 本地已存在。
