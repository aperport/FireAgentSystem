# 2026-09-09 工作日志

## 拉取远程 82d7b6a（半截重构，用户接手自改）

`git pull origin feature/dev` → `4dd3c05..82d7b6a`（fast-forward）

远程提交 `refactor(vector_db): align docstrings with BGE-M3 dual-vector write and drop in-memory BM25 retrieval`
只改了 `db_retriever.py` / `collections.py` / `db_operator.py` 的 docstring 与 BM25 删除，**调用方一个都没同步**。

### 拉取后确认的破坏面（供后续修复参照）
1. `db_retriever.hybrid_search()` 仍调 `self.bm25_search()` → AttributeError（hybrid 是默认路径）
2. `vector_retriever.search(search_type="sparse")` 仍调 `bm25_search`
3. `orchestrator._BM25Index.get()` 仍调 `rebuild_bm25_index()`
4. `knowledge_tools.py` 仍 import/使用 `_BM25Index`
5. **静默功能退化**：被删的那段全表加载代码同时是 `parent_map` 的唯一数据源，
   现在 `parent_map` 恒为空 → `context_fusion.fuse()` 的父文档回填失效（不报错、无效果）
6. `DENSE_SEARCH_SQL` / `SPARSE_SEARCH_SQL` 的 SELECT 缺 `source_name` 列，
   但 dense_search 里用 `row.get("source_name", "")` → 该字段永远为空（既有 bug）
7. 维度 1024 / 250002 在 DDL、db_operator、db_retriever 三处硬编码

### 我的处理（用户接手，可能丢弃）
改了 8 个源码文件：实现 `sparse_search()`（sparsevec SQL 检索 + `_to_sparsevec_literal` 索引 +1）、
删除 BM25 残留、补 `rebuild_parent_map()`、`_BM25Index` → `_RetrievalModule`、
collections 新增 `DENSE_VECTOR_DIM` / `SPARSE_VECTOR_DIM` 常量并让 DDL 引用。
`py_compile` 通过；可收集的 81 个测试全过。

**用户明确表示剩余部分自己改**，未提交、未推送。

## 后续：用户要求丢弃本地改动

`git fetch origin feature/dev && git reset --hard origin/feature/dev` → HEAD 回到 `82d7b6a`，
上述 8 个源码文件的本地改动**全部丢弃**（与远程完全一致）。
未执行 `git clean`，`.codebuddy/memory/` 下未跟踪的日志文件保留。
注意：`MEMORY.md` 是已跟踪文件，其本地修改也被一并覆盖。

### 环境事实
- 测试收集阶段 5 个 ERROR 均为缺依赖（fastmcp / pdf_inspector / langchain_mcp_adapters / websockets.sync），
  与代码改动无关，属既有环境问题
- `jieba` / `rank-bm25` 在 BM25 移除后已无任何代码引用，requirements 中成为孤儿依赖

## 用户接手后再次委托：实现 sparse_search（本轮会话）

用户在自己改的基础上（`_query_sparse_list` 拼字面量 + `sparse_search` 留了半截错误 SQL），
要求完成这两个方法，少防御性编程。本轮改动：

1. `db_retriever.py`：
   - `_query_sparse_list`（拼字符串字面量）→ `_query_sparse_vector`：直接用
     `encode_query_sparse()` + `pgvector.psycopg2.SparseVector`（与入库侧 db_operator 同规则：
     Top-128 截断、token id+1、dim 250002），删掉手写字面量拼接和 get_embedder 直调
   - `sparse_search()`：改用 `SPARSE_SEARCH_SQL` + `SPARSE_VECTOR_PLACEHOLDER` 模板
     （占位符 `%s::sparsevec`），cursor 绑定 SparseVector 对象（register_vector 已注册），
     无 try/except 包裹（少防御性编程）；`search_type="sparse"`；过滤 score_threshold
   - `hybrid_search()`：`self.bm25_search(...)` → `self.sparse_search(..., top_k=expand_k)`
     （修复 AttributeError 断链）；RRF 来源名 "bm25" → "sparse"；日志同步
2. `vector_retriever.py`：`search_type="sparse"` 分支从 `bm25_search(query, top_K=...)`
   改为 `sparse_search(query, top_k=..., category=..., score_threshold=...)`
3. imports 清理：删冗余 `from ingestion.embedding import get_embedder`（错误路径）、
   `_ASSERT_UNUSED` 占位元组；新增 `SparseVector`、`encode_query_sparse` 导入

已知未处理（沿袭记忆中既有问题，未越界修）：
- `hybrid_search` 仍把 category 传给 dense 而没传给 sparse（sparse 路线不带分类过滤）
- `SPARSE_SEARCH_SQL` SELECT 缺 `source_name` 列 → `row.get("source_name","")` 恒空（既有 bug）
- `dense_search` 仍有 try/except 包裹（用户只让改稀疏两方法，未动）

## 追加修复：source_name 列 + hybrid 分类过滤透传

用户要求修复上轮遗留的两项：
1. `collections.py`：`DENSE_SEARCH_SQL` / `SPARSE_SEARCH_SQL` 两个模板 SELECT 均补上
   `source_name` 列（dense 也缺，一并修）；
   `SPARSE_SEARCH_SQL` 上方"检索预留"注释改为已启用表述
2. `db_retriever.py` `hybrid_search()`：`sparse_search(...)` 透传
   `category=category, score_threshold=score_threshold`，两路分类过滤对齐；
   docstring 中"BM25 关键词检索"改为"稀疏向量检索"

验证：py lint 无错误，ast.parse（utf-8-sig）通过。
注意：`collections.py` 文件头有 UTF-8 BOM（既有），命令行单文件解析需用 utf-8-sig，
Python import 不受影响。

## 追加：graph_rag 全模块 docstring 一致性审查（用户委托的核对任务，结论已交付）

### A. 运行时断链（最严重）
- `orchestrator.py:78` 仍调用 `rebuild_bm25_index()`，HybridRetrievalModule 已无此方法
  → AttributeError。用户修复只改了 db_retriever/collections/vector_retriever 三文件，
  orchestrator 未同步（git modified 列表佐证）。

### B. docstring 过时清单
1. `graph_rag/__init__.py`：:14 "BM25+RRF"（实际 sparsevec）；:37 "config.py 空骨架"
   （实际已实现且 evaluator 在用）；:22 "evaluator 缺 ragas 导入"（实际完整）；
   :32 "embedding.py 骨架"（实际 BGEM3EmbeddingsAdapter 已实现）
2. `db_retriever.py` 模块 docstring 仍称 "sparse_search 是桩、BM25 生效"
3. `vector_retriever.py`：:14 "sparse→bm25_search()"、:77 "BM25 关键词检索"、
   :29/:33 BM25 索引相关待办——全部过时
4. `ingestion/entity_relation_extractor.py` docstring 引用不存在的 validate_extract_result
5. `json_save.py` :14 "裸 except" 已修复未删标注；:15 路径拼接问题仍属实
6. `context_fusion.py` :8 "回填完整父文档" 与实际（仅回填相邻 ±1 chunk）不符

### D. 顺带代码问题
- evaluator.py RAGASResult.print_result()/save_result() 约 40 行重复；
  ReferenceFreeEvaluation 未传 embeddings（AnswerRelevancy 会报错）

### 抽查一致
entity_extractor.py / config.py / evaluator.py / context_fusion 其余部分
docstring 与实现相符，无需改动。

（报告已交付，B 类修复待用户确认是否执行）

## 执行 B 类 docstring 修复（用户确认"只修改 docstring"）

按上轮 B 清单落地，仅改 docstring/注释，未动代码逻辑：

1. `db_retriever.py` 模块 docstring：删除 "sparse_search 是桩、BM25 生效、rebuild_bm25_index 生命周期"、
   sparsevec 路线区块改"已实装并启用"；`sparse_search()` docstring 去掉"替代内存 BM25"
2. `collections.py` 顶部：检索策略改为 dense/sparse(PG sparsevec)/hybrid 三路均生效；
   text 字段注释去掉"内存 BM25 语料来源"；路线区块"写入已实装，检索未启用"→"已实装并启用"
3. `db_operator.py`：删除 "以及 db_retriever.rebuild_bm25_index() 重建 BM25 索引" 一句
4. `vector_retriever.py`：sparse→bm25_search() 改为 sparse_search()；删除 BM25 索引相关已知问题/待办

未动的代码残留（用户要求只改 docstring，后续如需处理）：
- `orchestrator.py:78` `_BM25Index.get()` 仍调 `rebuild_bm25_index()` → 运行 AttributeError 断链（A 类，未授权）
- `db_retriever.initialize()` 仍含 BM25 构建残留 + `_build_parent_map()` 依赖 `bm25_corpus_docs`（未被调用，死代码）
- 行内注释 `collections.py:140-141`、`implementation-roadmap.md` 仍提及 BM25（非 docstring，未动）

## 记录两处问题并推送

- 新建 `docs/known-issues-20260909.md`，记录两处代码残留：
  1. `orchestrator.py:78` `_BM25Index.get()` 调已删除的 `rebuild_bm25_index()` → AttributeError 断链
  2. `db_retriever.initialize()` / `_build_parent_map()` BM25 构建死代码（`bm25_corpus_docs`）
- commit `77a887c` `docs(graph_rag): 对齐 sparsevec 检索相关 docstring 并记录两处遗留问题`，
  含 5 个源码文件 docstring 改动 + 新文档，已 push 到 `origin/feature/dev`（82d7b6a..77a887c）。
- `.codebuddy/memory/` 未纳入提交（未跟踪）。
