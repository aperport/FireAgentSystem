# 2026-09-02

## git pull（feature/dev：8cd9957 → 63a6d8c，快进合并）
- `doc_parser/dispatcher.py`：`parse_router()` 整个函数被注释掉（暂挂）。
- `doc_parser/md_parser.py`：`class NormalizeMD()` → `class NormalizeMD`（去掉空括号）。
- `doc_parser/pdf_parser.py`：`ScanPdfTransformer.pdf_extract()` 不再 return markdown，改为写入 `self.md`；新增 `pdf_write_md()`（md 为空则抛 `ValueError`）。
  - ⚠️ 现有测试 `src/test/graph_rag_test/ingestion_test/pdf_parser_test.py::test_pdf_extract` 仍写 `transformer.pdf_extract().strip() == result`，现在会拿到 `None` → AttributeError。待用户确认后同步改测试或让方法返回值。
- `ingestion/save_data.py`：删除末尾关于已删 `main()` 的注释残留。

## 清理
- 删除上次会话遗留的 `.git/COMMIT_MSG_1.txt`、`COMMIT_MSG_2.txt`。
