# 2026-09-05

## git pull（feature/dev：2182824 → 615af33，快进合并）

### 1. entity_extractor.py — 抽取器拆分（进行中，策略模式雏形）
- 新增 `NerEntityExtractor`：`_sync_extract()` + `extract()`（asyncio.to_thread 包装 BERT 推理）。
- 新增 `LlmEntityExtractor`：`_build_extract_prompt()` / `_format_schema()` / `extract()`（`with_structured_output(ExtractResult)`）。
- 老类 `EntityExtractor` 保留原样，尚未改为组合这两个类；两个新类当前**无任何调用方**。

#### 待确认的小问题（未改动）
- `LlmEntityExtractor.extract()` docstring 写“返回 ExtractResult”，实际 `return entities_list`（只取了 response.entities），与标注 `-> list` 一致但与 docstring 不符。
- 类属性 `NODE_TYPES = NODE_TYPES` / `REL_TYPES = REL_TYPES`（同名遮蔽式赋值）未被使用，`_format_schema()` 直接引用模块级常量 → 冗余。

### 2. ingestion/embedding.py
- 移除未使用的 `import os`。

### 延续待办（仍未处理）
- `pdf_parser_test.py::test_pdf_extract` 与 `pdf_extract()` 不返回值不兼容（2026-09-02 起）。
- `.vscode/settings.json` 仍被跟踪，未提交（`commentTranslate.multiLineMerge`），建议 `git rm --cached`。
- `graph_traverser.py:58` 有自己的 `get_neo4j_driver()`，与 `connection.py:145` 的新 `@lru_cache` 版本重复，未收敛。
- `writer.py` 的 `write_nodes(..., extra_props: dict[str, dict] = {})` 使用可变默认参数（Python 反模式）。

## entity_extractor 重构处于半成品状态（已按用户要求原样推送）
发现 WIP 后已向用户报告风险，用户明确表示「推送就好，我回去自己继续改」，故按现状提交并推送。
- `a3e687a chore(graph_rag)`：connection.py / schema.py / collections.py 的清理与格式整理
- `3428312 refactor(graph_rag)`：实体抽取拆分（WIP），提交信息中已逐条列出 4 个断点，方便接续
- 用户偏好：接受把 WIP 推到 feature/dev，遇到半成品不必拦截，说明风险后照推即可。

删除了 `entity_extractor.EntityExtractor` 类，改为 `NerEntityExtractor` / `LlmEntityExtractor` / `EntityFusionService` + 模块级 `main_pip()`，但调用方未同步：

1. `orchestrator.py:24` `from graph_rag.entity_extractor import EntityExtractor` → ImportError
2. `mcp_server/tools/knowledge_tools.py:31` 同上 → ImportError（MCP 工具服务起不来）
3. `ingestion/entity_relation_extractor.py:149` `EntityExtractor._is_similar(...)` → NameError（该文件只导入了 Entity, ExtractResult）
4. `entity_relation_extractor.py:39` `DocumentEntityExtractor()` 已去掉父类，但 51 行仍 `super().__init__(llm_client=..., query=...)` → TypeError；61-62 行 `self.NODE_TYPES` / `self.REL_TYPES`、137/140 行 `extractor.main_pip()` 均失效
5. 新的 `main_pip()` 内部调用的 `entity_extract_llm()` / `predict()` / `merge_results()` 在模块中均未定义 → 调用即 NameError

三个可选修复方向：
- A 最小改动：保留 `EntityExtractor` 作为兼容门面（组合三个新类），三处调用方都不用改
- B 完整跟进：改造 orchestrator / knowledge_tools / entity_relation_extractor 使用新组件，并补完 `main_pip`
- C 拆分提交：先只推 connection.py / schema.py / collections.py 这些已完成的整理，WIP 留在本地

本轮其它（可安全提交的）改动：
- `graph_db/connection.py`：删除大段注释掉的 Graphnode/GraphRelation dataclass 草稿，给 `get_neo4j_driver` 加分区注释
- `graph_db/schema.py`：节点 dataclass 之间的空行规范化
- `vector_db/collections.py`：模块 docstring 补充三条重构待办，替换掉已过时的"[简化理由]"注释，调整 `get_pg_instance` 换行
