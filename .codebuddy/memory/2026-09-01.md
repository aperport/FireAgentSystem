# 2026-09-01

## git pull（feature/dev：81b33ec → 5901b74，快进合并）
- `doc_parser/__init__.py`：类型标注现代化，`List/Dict` → `list/dict`。
- `doc_parser/pdf_parser.py`：删除未实现的模块文档字符串骨架，仅保留 `import pdf_inspector` 和 logger。
- `doc_parser/dispatcher.py`：文件末尾被追加了一个裸 `class`（第 41 行，无类名、无缩进块，且文件无结尾换行）→ **SyntaxError**，任何 import 该包的代码都会崩溃。已向用户报告，等待确认后删除该行。

## 提交并推送（5901b74 → 8cd9957，已 push 到 origin/feature/dev）
- 用户随后自行补全了 `dispatcher.py`：`file_geuss()`（基于 `filetype` 识别真实类型）+ `parse_router()`（后缀 → 引擎路由）。
- `feat(graph_rag)` e113618：dispatcher 新增类型识别与路由 + 测试 `test_file_geuss` + `requirements.txt` 补 `filetype>=1.2.0`。
- `style(graph_rag)` 8cd9957：`entity_extractor.py` / `evaluator.py` 按 PEP8 格式化，精简模块头文档字符串，evaluator 预留 `ReferenceFreeEvaluation` / `ReferenceBasedEvaluation` 占位类。
- 修正：`docs/project-audit-20260805.md` 中 `dispatcher.py` 名称被全局替换误删（2 处），已恢复后一并提交。

### 经验（Windows/PowerShell）
- `git commit -m "中文"` 在 PowerShell 下会报“字符串缺少终止符”（编码问题）。改用 `git commit -F <消息文件>`，写完删除临时文件。
- `.codebuddy/memory/*.md` 未加入 `.gitignore`，长期处于 untracked，提交时需用显式路径 add，避免误提交工作记忆。
