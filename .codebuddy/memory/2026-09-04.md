# 2026-09-04

## git pull（feature/dev：2608304 → 80ba7b2，快进合并）

### 1. graph_db/graph_vector_traverser.py — 依赖倒置重构
- `GraphVectorTraverser.__init__` 由 `(query, extract_result, neo4j_driver, embedder, top_k, max_paths_per_entity, score_threshold)` 改为注入三个协议组件：
  - `GraphPathTraverserProtocol`（`two_hop_traverse`，实现为同文件的 `GraphPathTraverser`）
  - `PathTextFormatterProtocol`（`paths_to_texts`，实现为 `PathTextFormatter`，staticmethod）
  - `VectorRerankerProtocol`（`rank_by_similarity`，实现为 `VectorReranker`）
- `search()` 改为 `search(query, extract_result)`，参数从构造期移到调用期。
- ⚠️ 当前代码库中**无任何实例化调用方**，故不会立刻报错；模块 docstring（14-15 行）仍是旧接口说明，待更新。

### 2. graph_db/schema.py — 删除 11 个关系 dataclass
- 仅删关系类，节点 dataclass（ModuleNode/FunctionNode/…/ZoneNode）保留。删除理由注释留在文件末尾（170-176 行）。

### 3. ingestion/entity_relation_extractor.py — validate_extract_result 迁出 schema
- `validate_extract_result()` 从 `graph_db/schema.py` 移到本模块末尾，改为本地定义；`from graph_db.schema import validate_extract_result` 已移除，改为 `from graph_db.schema import NODE_TYPES, REL_TYPES`。
- ⚠️ 文件头 docstring 第 8 行仍写「validate_extract_result → graph_db.schema」，已过时。
- 已确认 src 全仓无其它方从 schema 导入该函数，无 ImportError 风险；三文件语法检查通过。

### 后续：更新两处过时 docstring（未提交）
- `graph_db/graph_vector_traverser.py` 模块 docstring：旧接口 `GraphVectorTraverser(query, extract_result, neo4j_driver, embedder).search()` → 改为列出三个可注入 Protocol 组件及其默认实现类，并写成 `GraphVectorTraverser(traverser, formatter, reranker).search(query, extract_result)`。
- `ingestion/entity_relation_extractor.py` 模块 docstring：`validate_extract_result → graph_db.schema` → 改为 `NODE_TYPES / REL_TYPES → graph_db.schema` + `validate_extract_result → 本模块（见文件末尾）`。
- 改动仅限注释/docstring，语法检查与 lint 均通过，未提交。
