# 2026-09-08 工作日志

## BGE-M3 Embedding 迁移修复（全部完成）

修改文件：config.py、embedding.py、db_operator.py、db_retriever.py、graph_vector_traverser.py、evaluator.py、collections.py、requirements.txt

要点：
- config 模型名 U+2011 → ASCII `BAAI/bge-m3`
- embedding.py：BGEM3FlagModel(model_name_or_path, use_fp16=False, devices=[...])；encode_query_dense / encode_query_sparse / encode_hybrid / BGEM3EmbeddingsAdapter
- db_operator：Vector/SparseVector 写入，稀疏 Top-128 截断，索引 +1（sparsevec 从 1 起），总维 250002
- db_retriever：dense_search 用 encode_query_dense；HybridRetrievalModule 移除 embedder 参数
- graph_vector_traverser：VectorReranker 用 get_embedder() 单例
- evaluator：BGEM3EmbeddingsAdapter 替代 HuggingFaceEmbeddings
- collections：fire_doc dense 512→1024，sparse 30522→250002
- requirements：+ FlagEmbedding>=1.2.0

lint 全绿；测试无 embedder 旧接口引用。

遗留：旧 PG 表若按 512 维建需重建；sparse_search 仍为预留。

## 提交并推送到 GitHub

- 提交 4dd3c05 `feat(graph_rag): 迁移 Embedding 至 BGE-M3 并修复 pgvector 稠密/稀疏向量写入`，11 文件
- 已 push 到 origin/feature/dev（远端 https://github.com/aperport/FireAgentSystem.git，b608c91..4dd3c05）
- 刻意未纳入提交：`.vscode/settings.json`（IDE 本地配置，且 .gitignore 已含 .vscode/）与 `.codebuddy/memory/*.md`（含本机绝对路径，建议后续加入 .gitignore）
- 提交前已 py_compile 全量校验变更文件，无语法错误
