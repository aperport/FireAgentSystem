# 2026-08-31

## 提交并推送 md_parser 重构
- 提交 `81b33ec` 已推送到 `origin/feature/dev`（远程 GitHub: aperport/FireAgentSystem）
- 内容：
  - `src/graph_rag/ingestion/doc_parser/md_parser.py`：`MetadataEnhancer` 改为 `Protocol`；新增 `Normalize`/`NormalizeMD` 协议与实现类；`_normalize_headers`/`_normalize_whitespace`/`_extract_images`/`_build_base_metadata`/`_infer_category` 等私有方法提取为模块级 `extract_images`/`build_base_metadata` 函数
  - `src/graph_rag/graph_db/__init__.py`：删除过时的模块文档字符串
  - `.vscode/settings.json`：添加 `commentTranslate.source` 配置
- 注意：`.vscode` 目录在 .gitignore 中，但 `settings.json` 已被跟踪，更新需用 `git add -f`
