# 2026-09-11

## memory_update.py 记忆中间件代码走读（用户询问，未改代码）

用户连续追问 `src/agent/middlewares/memory_update.py` 的机制，走读结论：

### 架构全貌
- **写路径**：`MemoryUpdateMiddleware`（`aafter_agent` 钩子）自动提取实体 → 合并 → 写 StoreBackend。
- **读路径**：无加载函数。`ContextInjectionMiddleware`（`before_agent`）注入 SystemMessage 提示 Agent 自己 `read_file` 读 `/memories/{user_id}/preferences.md`。
- **路由**：`main_agent.py:114` CompositeBackend 将 `/memories/` 前缀路由到 StoreBackend（namespace 取 `runtime.context.user_id`），底层 STORE 为 PostgresStore（`config.py:57`）。
- **存储 key**：namespace `(user_id,)` + key `/{user_id}/preferences.md`。

### 不会无限叠加
`_merge_preferences` 是"删旧块→去重合并→截断→追加"：equipment 上限 10、zones 上限 5、queries 上限 5。三个区块之外的内容原样保留（理论上可增长，但无写入来源）。

### 用户记录的待优化问题（用户说后续自己优化）
1. **`preferred_*` 偏好字段是"纸面设计"**：`AGENTS.md` 和 `schema.UserPreferences` 定义了 `preferred_output / preferred_chart_type / preferred_language`，但：
   - `_extract_entities` 提示词只提取 equipment/zones/query，不提取偏好
   - 主系统提示词（`prompts.py`）完全没提偏好写入流程
   - `UserPreferences` dataclass 无生产代码引用，仅测试使用 → 死代码
   - 实际文件内容只是"近期活动记录"而非"偏好文件"
   - 修复方向：提取提示词加偏好字段，或 AGENTS.md/系统提示词明确"发现偏好→确认→写入"流程
2. **`AGENTS.md:113-123` 偏好文件示例缺 `recent_zones` 区块**，与中间件实际写入格式不一致
3. **`memory_update.py:184` 手动 `find("{")` 抠 JSON**，而文件头注释说"使用 with_structured_output"——注释与实现不符，可改用结构化输出
4. **`memory_update.py:246` 每次 `aafter_agent` 都重新实例化 `MemoryUpdateMiddlewareTools`**，可移到 `__init__` 或模块级
5. `_is_meaningful_last` 的关键词/跳过词表硬编码在类里，若调整需改代码
